{
  "type": "{{ document.type }}",
  "uuid": "{{ document.uuid }}",
  "attributes": {
    "title": "{{ document.title }}!",
    "body": "{{ document.body }}",
    "created": "{{ document.created }}",
    "updated": "{{ document.updated }}"
  }
}
